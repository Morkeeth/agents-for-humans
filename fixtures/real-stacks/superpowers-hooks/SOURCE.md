# Source · fixtures/real-stacks/superpowers-hooks

Extracted 2026-09-10 from live clone:
`https://github.com/obra/superpowers` → `hooks/hooks.json` + `hooks/hooks-cursor.json`.

Purpose: offline object for `hooks-layout` probe. Not a full Superpowers stack.
Re-derive against a fresh clone with:
`magnet probe hooks-layout --stack /path/to/superpowers`

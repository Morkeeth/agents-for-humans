# Real-stack snapshot · Agent Grinder

Source object: `Morkeeth/agentgrinder` `skills/agentgrinder/SKILL.md`
(cloned and copied into this fixture for cold-path stranger runs — no network).

Do not invent coverage numbers here. Re-derive:

```bash
magnet probe stack-coverage --stack fixtures/real-stacks/agentgrinder
magnet stack --stack fixtures/real-stacks/agentgrinder
```

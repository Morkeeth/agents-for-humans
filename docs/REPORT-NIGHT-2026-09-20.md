# REPORT · Night 2026-09-20 · MAGNET S55–S57

## SHIPPED

1. **S55** bare `three to four` / `3 to 4` destinations · bare `twenty percent` / `20%` rise.
2. **S56** `-twenty percent` / `minus twenty percent` fall · word arrows `three → four`.
3. **S57** `exactly four` / `falls to four` / `exactly twenty` word targets.
4. Reports S55–S57; suite **462** tests; pred-demo **172/172** · embarrassed **86**.

## VERIFIED

| Claim | Command | Result |
|-------|---------|--------|
| Tip | `git rev-parse HEAD` after push | stamped in LOG |
| Suite | `python3 -m pytest -q` | **461 passed, 1 skipped** |
| Docs | `magnet check-docs` | **15 PASS** |
| Pred-demo | `magnet pred-demo` | **172/172** · embarrassed **86** · FINDING |
| Judge | `bash scripts/judge-demo.sh` | JUDGE DEMO OK |

## WRONG

- First bare-`%` draft stole `100%` into percent — caught by running `@latest=4` before ship.
- Assumed `from three to four` closed the destination surface; bare / arrow / exactly-word still lied.
- `fifteen percent` / `must be four` (no exactly) still unbound.
- PR create pending user approval; could not `git push origin main` (Oscar click / cloud PR workflow).
- Bedrock still blocked; film / Devpost untouched.
